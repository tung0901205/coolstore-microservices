# ============================================================
# AUTH SERVICE - application.properties (?Ã S?A L?I)
# Port: 8089
# S?A: ??i hibernate generation sang drop-and-create cho dev
#      ?? t? t?o sequence app_user_seq
# ============================================================

%dev.quarkus.http.port=8089

# CORS
quarkus.http.cors=true
quarkus.http.cors.access-control-allow-credentials=true
quarkus.http.cors.origins=http://localhost:4200
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS

# PostgreSQL
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.max-size=8
quarkus.datasource.jdbc.min-size=2

# Dev: k?t n?i PostgreSQL local (Docker port 5432, database: coolstore)
%dev.quarkus.datasource.username=postgres
%dev.quarkus.datasource.password=postgres
%dev.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/coolstore

# Prod
%prod.quarkus.datasource.username=quarkus
%prod.quarkus.datasource.password=quarkus
%prod.quarkus.datasource.jdbc.url=jdbc:postgresql://auth-database:5432/coolstore

# ============================================================
# QUAN TR?NG: Dùng drop-and-create cho dev
# Lý do: AppUser dùng @SequenceGenerator(sequenceName = "app_user_seq")
# Mode "update" KHÔNG t?o sequence m?i ? l?i khi ??ng ký user
# Mode "drop-and-create" t?o l?i toàn b? schema + sequence m?i l?n start
# ============================================================
%dev.quarkus.hibernate-orm.database.generation=drop-and-create
%dev.quarkus.hibernate-orm.sql-load-script=import.sql
%prod.quarkus.hibernate-orm.database.generation=update

quarkus.hibernate-orm.log.sql=false
quarkus.hibernate-orm.sql-load-script=import.sql

# JWT - các file key ?ã có s?n trong resources/
smallrye.jwt.sign.key.location=privateKey.pem
mp.jwt.verify.publickey.location=publicKey.pem
mp.jwt.verify.issuer=https://coolstore.vn

coolstore.jwt.duration=86400
coolstore.jwt.issuer=https://coolstore.vn

# Swagger UI
quarkus.swagger-ui.always-include=true